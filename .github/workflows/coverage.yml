name: Test Coverage

on:
  workflow_call:
    inputs:
      modules:
        required: true
        type: string
      go-version:
        required: true
        type: string
      golangci-lint-version:
        required: true
        type: string

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    timeout-minutes: 10

    permissions:
      contents: write
      pages: write

    steps:
      # tests
      - name: Checkout code
        uses: actions/checkout@v4.2.2

      - name: Install Go
        uses: actions/setup-go@v5.4.0
        with:
          go-version: ${{ inputs.go-version }}

      - name: Verify dependencies
        run: go mod verify

      - name: Restore cache
        uses: actions/cache@v4.2.0
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-v1-go-${{ hashFiles('**/go.sum', 'go.work.sum') }}
          restore-keys: |
            ${{ runner.os }}-v1-go-

      - name: Check and install dependencies
        run: |
          # Находим все go.mod файлы и выполняем go mod tidy в каждой директории
          find . -name "go.mod" -type f | while read -r modfile; do
            dir=$(dirname "$modfile")
            echo "Processing module in: $dir"
            cd "$dir"
            go mod tidy
            cd - > /dev/null
          done
          
          # Проверяем изменения в go.mod и go.sum файлах
          git diff --exit-code **/go.mod
          git diff --exit-code **/go.sum

      - name: Install gocov-html
        run: |
          go install github.com/axw/gocov/gocov@v1
          go install github.com/matm/gocov-html/cmd/gocov-html@v1

      - name: Run tests
        run: |
          mkdir -p .github_pages
          echo "mode: atomic" > coverage.out
          
          # Собираем все пакеты из всех модулей для правильного покрытия
          echo "🔍 Collecting all packages from all modules..."
          all_packages=()
          modules_found=()
          
          while IFS= read -r modfile; do
            dir=$(dirname "$modfile")
            echo "📦 Processing module in: $dir"
            cd "$dir"
            
            # Получаем список всех пакетов в модуле
            module_packages=$(go list ./... 2>/dev/null || echo "")
            if [ -n "$module_packages" ]; then
              modules_found+=("$dir")
              while IFS= read -r pkg; do
                # Исключаем mock пакеты 
                if [[ "$pkg" != *"/mocks"* ]] && [[ "$pkg" != *"/mocks/"* ]]; then
                  all_packages+=("$pkg")
                fi
              done <<< "$module_packages"
              echo "✅ Added $(echo "$module_packages" | wc -l) packages from $dir"
            else
              echo "⚠️ No packages found in $dir"
            fi
            
            cd - > /dev/null  
          done < <(find . -name "go.mod" -type f)
          
          echo "📊 Total packages found: ${#all_packages[@]}"
          echo "📁 Modules with packages: ${modules_found[*]}"
          
          # Показываем найденные пакеты для отладки
          if [ ${#all_packages[@]} -gt 0 ]; then
            echo "🔍 All packages to cover:"
            printf '%s\n' "${all_packages[@]}" | head -10
            if [ ${#all_packages[@]} -gt 10 ]; then
              echo "... and $((${#all_packages[@]} - 10)) more"
            fi
          fi
          
                    # Создаем строку с пакетами для -coverpkg параметра
          if [ ${#all_packages[@]} -gt 0 ]; then
            # Объединяем все пакеты через запятую для -coverpkg
            coverpkg_list=$(IFS=','; echo "${all_packages[*]}")
            echo "📋 Coverage package list: $coverpkg_list"
            
            # Запускаем тесты в каждом модуле с полным покрытием всех пакетов
            modules_with_tests=0
            total_modules=0
            
            for module_dir in "${modules_found[@]}"; do
              echo "🔍 Running tests in module: $module_dir"
              total_modules=$((total_modules + 1))
              cd "$module_dir"
              
              # Проверяем наличие тестовых файлов
              if find . -name "*_test.go" -type f | grep -q .; then
                echo "📝 Found test files in $module_dir"
                
                                 # Запускаем тесты с покрытием всех пакетов проекта
                 echo "🧪 Running: go test ./... -coverprofile=tmp_coverage.out -covermode=atomic -coverpkg=\"$coverpkg_list\""
                 if go test -v ./... -coverprofile=tmp_coverage.out -covermode=atomic -coverpkg="$coverpkg_list" 2>&1; then
                  echo "✅ Tests passed in $module_dir"
                  
                  if [ -f tmp_coverage.out ]; then
                    # Подсчитываем строки покрытия (исключая заголовок)
                    coverage_lines=$(tail -n +2 tmp_coverage.out | wc -l)
                    echo "📊 Coverage lines found: $coverage_lines"
                    
                    if [ "$coverage_lines" -gt 0 ]; then
                      modules_with_tests=$((modules_with_tests + 1))
                      
                      # Добавляем данные покрытия, исключая mock файлы и main.go
                      tail -n +2 tmp_coverage.out | while IFS= read -r line; do
                        file_path=$(echo "$line" | cut -d' ' -f1)
                        
                        # Пропускаем mock файлы и main.go  
                        if [[ "$file_path" == *"/mocks/"* ]] || [[ "$file_path" == *"cmd/main.go"* ]]; then
                          continue
                        fi
                        
                        echo "$line"
                      done >> ../../coverage.out
                      
                      echo "✅ Added coverage data from $module_dir"
                    else
                      echo "⚠️ No coverage data in $module_dir"
                    fi
                    
                    rm tmp_coverage.out
                  else
                    echo "❌ No coverage file generated in $module_dir"
                  fi
                else
                  echo "❌ Tests failed in $module_dir, but continuing..."
                fi
              else
                echo "ℹ️ No test files found in $module_dir"
              fi
              
              cd - > /dev/null
            done
            
            echo "📈 Processed $total_modules modules, $modules_with_tests with tests"
          else
            echo "❌ No packages found, cannot run coverage tests"
          fi
          
          # Удаляем дубликаты в файле покрытия, сохраняя заголовок
          if [ -f coverage.out ]; then
            coverage_lines_final=$(($(wc -l < coverage.out) - 1))
            echo "📊 Final coverage data lines: $coverage_lines_final"
            
            if [ "$coverage_lines_final" -gt 1 ]; then
              # Сортируем и удаляем дубликаты, сохраняя заголовок
              echo "🧹 Removing duplicates from coverage data..."
              (head -1 coverage.out; tail -n +2 coverage.out | sort -u) > coverage_clean.out
              mv coverage_clean.out coverage.out
              echo "✅ Coverage data cleaned"
            fi
          fi
          
          # Показываем информацию о файле покрытия
          echo "📊 Coverage file info:"
          ls -la coverage.out || echo "coverage.out not found"
          echo "Total lines in coverage.out: $(wc -l < coverage.out)"
          echo "Coverage data lines (excluding header): $(($(wc -l < coverage.out) - 1))"
          
          # Показываем первые несколько строк для отладки
          echo "🔍 First 5 lines of coverage.out:"
          head -5 coverage.out || echo "Cannot read coverage.out"
          
          # Генерируем отчеты только если есть данные покрытия
          if [ -s coverage.out ] && [ $(wc -l < coverage.out) -gt 1 ]; then
            echo "📊 Generating coverage reports..."
            
            # Генерируем функциональный отчет
            if go tool cover -func=coverage.out > cover.out 2>&1; then              
              echo "✅ Function coverage report generated"
              
              # Показываем итоговое покрытие
              echo "📈 Coverage summary:"
              cat cover.out | tail -5
              
              # Генерируем HTML отчет
              if gocov convert coverage.out | gocov-html > .github_pages/index.html 2>&1; then
                echo "✅ HTML coverage report generated"
                
                # Добавляем навигацию к HTML отчету покрытия
                sed -i '/<\/head>/i\
                <style>\
                .navigation {\
                  position: fixed;\
                  top: 10px;\
                  right: 10px;\
                  background: white;\
                  padding: 10px;\
                  border: 1px solid #ccc;\
                  border-radius: 5px;\
                  box-shadow: 0 2px 5px rgba(0,0,0,0.2);\
                  z-index: 1000;\
                }\
                .nav-link {\
                  display: inline-block;\
                  margin: 0 5px;\
                  padding: 5px 10px;\
                  background: #f6f8fa;\
                  color: #24292f;\
                  text-decoration: none;\
                  border-radius: 3px;\
                  border: 1px solid #d1d9e0;\
                  font-size: 12px;\
                }\
                .nav-link:hover {\
                  background: #e1e4e8;\
                }\
                </style>' .github_pages/index.html
                
                sed -i '/<body>/a\
                <div class="navigation">\
                  <a href="./" class="nav-link">📊 Coverage</a>\
                  <a href="./reports.html" class="nav-link">📋 All Reports</a>\
                </div>' .github_pages/index.html
                
              else
                echo "❌ Failed to generate HTML report"
                echo "<html><body><h1>Failed to generate coverage report</h1></body></html>" > .github_pages/index.html
              fi
            else
              echo "❌ Failed to generate function coverage report"
              cat cover.out || echo "No cover.out to display"
              echo "total:                  (statements)   0.0%" > cover.out
              echo "<html><body><h1>Failed to generate coverage report</h1></body></html>" > .github_pages/index.html
            fi
          else
            echo "⚠️ No coverage data available"
            echo "total:                  (statements)   0.0%" > cover.out
            echo "<html><body><h1>No coverage data available</h1><p>No tests found or no coverage generated</p></body></html>" > .github_pages/index.html
          fi
          
          # Показываем содержимое cover.out для отладки
          echo "🔍 Content of cover.out:"
          cat cover.out || echo "cover.out not found"

      - name: 🔗 Create main reports index
        run: |
          echo "🔗 Создание индекса отчетов..."
          
          # Создаем главную страницу с навигацией между отчетами
          cat > .github_pages/reports.html << 'EOF'
          <!DOCTYPE html>
          <html lang="ru">
          <head>
              <meta charset="UTF-8">
              <meta name="viewport" content="width=device-width, initial-scale=1.0">
              <title>📊 Reports Dashboard</title>
              <style>
                  body {
                      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
                      line-height: 1.6;
                      color: #24292f;
                      max-width: 1200px;
                      margin: 0 auto;
                      padding: 20px;
                      background: #ffffff;
                  }
                  .header {
                      text-align: center;
                      padding: 20px 0;
                      border-bottom: 2px solid #e1e4e8;
                      margin-bottom: 30px;
                  }
                  .reports-grid {
                      display: grid;
                      grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
                      gap: 20px;
                      margin: 30px 0;
                  }
                  .report-card {
                      background: #f6f8fa;
                      border: 1px solid #d1d9e0;
                      border-radius: 8px;
                      padding: 20px;
                      text-align: center;
                      transition: box-shadow 0.2s;
                  }
                  .report-card:hover {
                      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
                  }
                  .report-link {
                      display: block;
                      color: #0969da;
                      text-decoration: none;
                      font-weight: 600;
                      font-size: 18px;
                      margin: 10px 0;
                  }
                  .report-link:hover {
                      text-decoration: underline;
                  }
                  .report-desc {
                      color: #656d76;
                      font-size: 14px;
                  }
                  .info {
                      background: #f0f9ff;
                      color: #0369a1;
                      padding: 12px 20px;
                      border-radius: 8px;
                      border: 1px solid #bae6fd;
                      margin: 20px 0;
                      text-align: center;
                  }
              </style>
          </head>
          <body>
              <div class="header">
                  <h1>📊 MSA Rocket Factory - Reports Dashboard</h1>
              </div>
              
              <div class="info">
                  <strong>Последнее обновление:</strong> $(date)<br>
                  <strong>Commit:</strong> ${{ github.sha }}<br>
                  <strong>Branch:</strong> ${{ github.ref_name }}
              </div>
              
              <div class="reports-grid">
                  <div class="report-card">
                      <h2>📊</h2>
                      <a href="./" class="report-link">Coverage Report</a>
                      <p class="report-desc">Отчет о покрытии кода тестами с детальной информацией по файлам и функциям</p>
                  </div>
                  
                  <div class="report-card">
                      <h2>🧪</h2>
                      <a href="./tests/" class="report-link">Test Report</a>
                      <p class="report-desc">Результаты выполнения unit тестов по всем модулям проекта</p>
                  </div>
                  
                  <div class="report-card">
                      <h2>🔍</h2> 
                      <a href="./lint/" class="report-link">Lint Report</a>
                      <p class="report-desc">Результаты статического анализа кода с помощью golangci-lint</p>
                  </div>
              </div>
              
              <div style="text-align: center; margin-top: 40px; color: #656d76;">
                  <p>🚀 Generated by MSA Rocket Factory CI/CD Pipeline</p>
              </div>
          </body>
          </html>
          EOF
          
          echo "✅ Главная страница отчетов создана"

      # pages
      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload Artifact for Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: .github_pages/

      - name: Upload Coverage Report as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: |
            .github_pages/
            coverage.out
            cover.out
          retention-days: 30

      # badge - обновляем только для main ветки
      - name: Update coverage Badge
        if: github.ref == 'refs/heads/main'
        uses: tj-actions/coverage-badge-go@v2
        with:
          filename: cover.out
          green: 80
          yellow: 60
          target: README.md

      - name: Verify Changed files
        if: github.ref == 'refs/heads/main'
        uses: tj-actions/verify-changed-files@v12
        id: verify-changed-files
        with:
          files: README.md

      - name: Debug coverage info
        if: github.ref == 'refs/heads/main'
        run: |
          echo "📊 Coverage files info:"
          echo "coverage.out:"
          ls -la coverage.out || echo "coverage.out not found"
          echo "cover.out:"
          ls -la cover.out || echo "cover.out not found"
          
          echo ""
          echo "📈 Raw coverage data (first 10 lines):"
          head -10 coverage.out || echo "Cannot read coverage.out"
          
          echo ""
          echo "📈 Function coverage summary:"
          cat cover.out || echo "Cannot read cover.out"
          
          echo ""
          echo "🔍 README badge section before update:"
          grep -A 2 -B 2 "coverage" README.md || echo "No coverage badge found in README.md"
          
          echo ""
          echo "🔍 README changes after badge update:"
          git diff README.md || echo "No changes in README.md"

      - name: Commit changes
        if: github.ref == 'refs/heads/main' && steps.verify-changed-files.outputs.files_changed == 'true'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add README.md
          git commit -m "chore: Updated coverage badge."

      - name: Push changes
        if: github.ref == 'refs/heads/main' && steps.verify-changed-files.outputs.files_changed == 'true'
        run: |
          git push origin HEAD:${{ github.ref_name }}

  coverage:
    needs: test
    runs-on: ubuntu-latest
    timeout-minutes: 10

    permissions:
      contents: write
      pages: write
      id-token: write

    # Деплоим только для main ветки
    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy coverage report to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        continue-on-error: true

      - name: Check deployment status
        if: steps.deployment.outcome == 'success'
        run: |
          echo "✅ Coverage report successfully deployed to GitHub Pages"
          echo "📊 Report available at: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}"

      - name: Deployment failed notice
        if: steps.deployment.outcome == 'failure'
        run: |
          echo "⚠️ GitHub Pages deployment failed"
          echo "📋 Coverage report is available as an artifact: coverage-report"
          echo "🔧 Possible solutions:"
          echo "   1. Check repository Settings > Pages > Source (should be 'GitHub Actions')"
          echo "   2. Verify environment protection rules allow this branch"
          echo "   3. Ensure repository has Pages enabled"
          echo "   4. Download artifacts to view coverage locally"

  # Для всех остальных веток и pull request только артефакт
  coverage-info:
    needs: test
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    # Запускаем только для pull request или не-main веток
    if: github.event_name == 'pull_request' || github.ref != 'refs/heads/main'
    
    steps:
      - name: Download coverage artifacts
        uses: actions/download-artifact@v4
        with:
          name: coverage-report
          path: coverage-report

      - name: Create simple HTML report
        run: |
          if [ -f coverage-report/cover.out ]; then
            echo "<!DOCTYPE html>" > coverage-report/simple-report.html
            echo "<html><head><title>Coverage Report</title>" >> coverage-report/simple-report.html
            echo "<style>body{font-family:Arial,sans-serif;margin:40px;} .coverage{background:#f5f5f5;padding:20px;border-radius:5px;}</style>" >> coverage-report/simple-report.html
            echo "</head><body>" >> coverage-report/simple-report.html
            echo "<h1>📊 Test Coverage Report</h1>" >> coverage-report/simple-report.html
            echo "<div class='coverage'>" >> coverage-report/simple-report.html
            echo "<h2>Coverage Summary:</h2>" >> coverage-report/simple-report.html
            echo "<pre>" >> coverage-report/simple-report.html
            cat coverage-report/cover.out >> coverage-report/simple-report.html
            echo "</pre>" >> coverage-report/simple-report.html
            echo "</div>" >> coverage-report/simple-report.html
            echo "<p><strong>Pull Request:</strong> #${{ github.event.pull_request.number }}</p>" >> coverage-report/simple-report.html
            echo "<p><strong>Branch:</strong> ${{ github.head_ref }}</p>" >> coverage-report/simple-report.html
            echo "<p><strong>Commit:</strong> ${{ github.sha }}</p>" >> coverage-report/simple-report.html
            echo "</body></html>" >> coverage-report/simple-report.html
          fi

      - name: Upload simple report
        uses: actions/upload-artifact@v4
        with:
          name: simple-coverage-report
          path: coverage-report/simple-report.html
          retention-days: 30

      - name: Coverage report info
        run: |
          echo "📊 Coverage report generated successfully"
          echo "📋 Report is available as an artifact: coverage-report"
          echo "🌐 To view the report on GitHub Pages, merge this PR to main branch"
          echo "📁 You can download the artifact to view the report locally"
          echo ""
          echo "📈 Coverage summary:"
          if [ -f coverage-report/cover.out ]; then
            cat coverage-report/cover.out | tail -1
          else
            echo "Coverage summary not available"
          fi