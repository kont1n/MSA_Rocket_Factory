type: object

required:
  - order_uuid

properties:

  order_uuid:
    allOf:
      - $ref: ./order_dto.yaml#/properties/order_uuid
      
  total_price:
    allOf:
      - $ref: ./order_dto.yaml#/properties/total_price
